<nav class="navbar navbar-expand-lg bg-primary fixed-top" data-bs-theme="dark">
  <div class="container-fluid">

    <a class="navbar-brand" href="/" style="font-size: 0.9em;"><img src="{{ asset("/images/logo.png") }}" alt="Logo du Primeur de Laigneville">  ACCUEIL</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Basculer la navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link vertical-align-inherit" href="{{ path("app_category") }}">Nos produits
            <span class="visually-hidden">(current)</span>
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link vertical-align-inherit" href="/service">Nos services</a>
        </li>
        <li class="nav-item">
          <a class="nav-link vertical-align-inherit" href="/promo">Bon plans</a>
        </li>
        <li class="nav-item">
          <a class="nav-link vertical-align-inherit" href="/apropos">À propos</a>
        </li>
        </ul>
        <ul class="navbar-nav me-auto">
        
        {% if app.user %}
        <li class="nav-item dropdown">
  <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
    <span class="vertical-align-inherit">{{ app.user.firstname}}</span>
  </a>
  <div class="dropdown-menu">
    <a class="dropdown-item" href="{{ path("app_logout") }}">
      <span class="vertical-align-inherit">Me déconnecter</span>
    </a>
    <a class="dropdown-item" href="{{ path("app_user", {id : app.user.id}) }}">
      <span class="vertical-align-inherit">Changer mon mot de passe</span>
    </a>
    <a class="dropdown-item" href="{{ path("app_home")}}">
      <span class="vertical-align-inherit"></span>
    </a>
    
    {% if 'ROLE_ADMIN' in app.user.roles %}
    <div class="dropdown-divider"></div>
    <a class="dropdown-item" href="{{ path('new_promo') }}">Créer une Promo</a>
    
    <div class="dropdown-divider"></div>
    <a class="dropdown-item" href="{{ path("admin") }}">
      <span class="vertical-align-inherit">Panneau administrateur</span>
    </a>
  </div>
</li>
  
            
            {% endif %}
            
            
        
      {% else %}
      <li class="navbar-nav me-auto">
          <a class="nav-link" href="{{ path('app_login')}}">Connexion</a>
          <a class="nav-link" href="{{ path('app_registration')}}">Inscription</a>
      </li>
      {% endif %}
        
      </ul>
      <div class="d-flex me-4">
    <a href="{{ path('app_liste')}}" class="text-light fw-bold">
        <i class="fa-solid fa-cart-shopping cart-icon" title="Ma liste"></i>
    </a>
</div>
      
      <form class="d-flex" action="{{ path('app_search') }}" method="get">
        <input class="form-control me-sm-2" type="search" name="q" placeholder="Recherche">
        <button class="btn btn-secondary my-2 my-sm-0 vertical-align-inherit" type="submit">Recherche</button>
      </form>
    </div>
  </div>
</nav>